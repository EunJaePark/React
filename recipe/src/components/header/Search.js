import React, { Fragment } from 'react';
// import styled from 'styled-components';

const Search = () => {
    return (
        <Fragment>
            search
        </Fragment>
    );
};

export default Search;